//
//  Development.xcconfig
//  Kingtherum
//
//  Development environment configuration
//  🚨 DO NOT COMMIT ACTUAL API KEYS TO GIT
//

// MARK: - API Configuration
// Replace these placeholders with actual values in your local development environment

// Infura Configuration
// Get your project ID from https://infura.io/dashboard
INFURA_PROJECT_ID = YOUR_INFURA_PROJECT_ID_HERE
INFURA_PROJECT_SECRET = YOUR_INFURA_PROJECT_SECRET_HERE

// Etherscan Configuration  
// Get your API key from https://etherscan.io/myapikey
ETHERSCAN_API_KEY = YOUR_ETHERSCAN_API_KEY_HERE

// MARK: - Build Settings
// These values will be injected into the app's Info.plist at build time

// Preprocessor macros to inject values into Info.plist
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) INFURA_PROJECT_ID=\"$(INFURA_PROJECT_ID)\" INFURA_PROJECT_SECRET=\"$(INFURA_PROJECT_SECRET)\" ETHERSCAN_API_KEY=\"$(ETHERSCAN_API_KEY)\"

// MARK: - Security Notes
// 1. Copy this file to Development-Local.xcconfig
// 2. Replace placeholders with actual API keys in the -Local version
// 3. The -Local version is ignored by git and safe for development
// 4. Never commit actual API keys to version control